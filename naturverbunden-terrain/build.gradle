archivesBaseName = "naturverbunden-terrain"
version = getSubprojectVersion(project)

dependencies {
	compile project(path: ':naturverbunden-core')
	compile project(path: ':naturverbunden-primitive')
}
