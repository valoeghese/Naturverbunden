archivesBaseName = "naturverbunden-primitive"
version = getSubprojectVersion(project)

dependencies {
	compile project(path: ':naturverbunden-core')
	compile project(path: ':naturverbunden-terrain')
}
