archivesBaseName = "naturverbunden-primitive"
version = getSubprojectVersion(project)

dependencies {
	implementation project(path: ':naturverbunden-core')
	implementation project(path: ':naturverbunden-zoesteria')
}
